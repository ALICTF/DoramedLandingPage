---
// src/pages/blog/index.astro
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';

const allPosts = (await getCollection('blog'))
    .filter(post => !post.data.draft)
    .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const featuredPost = allPosts[0];
const otherPosts = allPosts.slice(1);

const title = "مجله سلامت دورامد | تکنولوژی و پزشکی";
const formatDate = (date: Date) => new Intl.DateTimeFormat('fa-IR', { dateStyle: 'long' }).format(date);
---

<Layout title={title} description="مرجع تخصصی مقالات پزشکی هوشمند، تله‌مدیسین و سلامت دیجیتال">
    <Navbar />
    
    <main class="relative pt-32 pb-24 min-h-screen bg-gray-50 text-gray-800">
        
        <div class="absolute top-0 right-0 w-[600px] h-[600px] bg-gradient-to-b from-blue-50 to-transparent rounded-full blur-3xl opacity-60 pointer-events-none -z-10"></div>
        <div class="absolute top-1/2 left-0 w-[500px] h-[500px] bg-cyan-50/50 rounded-full blur-3xl pointer-events-none -z-10"></div>

        <div class="container mx-auto px-4 relative z-10">
            
            <div class="text-center max-w-3xl mx-auto mb-20">
                <span class="inline-block py-1.5 px-4 rounded-full bg-blue-100 text-doramed-primary text-xs font-bold border border-blue-200 mb-5">
                    مجله خبری و آموزشی
                </span>
                <h1 class="text-4xl md:text-6xl font-black text-gray-900 mb-6 tracking-tight leading-tight">
                    دنیای 
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-doramed-primary to-cyan-500">
                        سلامت هوشمند
                    </span>
                </h1>
                <p class="text-gray-500 text-lg leading-relaxed mb-10 max-w-2xl mx-auto">
                    جدیدترین مقالات علمی، اخبار تکنولوژی پزشکی و راهکارهای ارتقای سلامت را اینجا دنبال کنید.
                </p>

                <div class="relative max-w-lg mx-auto group">
                    <div class="absolute -inset-1 bg-gradient-to-r from-doramed-primary to-cyan-400 rounded-2xl blur opacity-20 group-hover:opacity-40 transition duration-500"></div>
                    <div class="relative bg-white border border-gray-100 rounded-2xl p-2 flex items-center shadow-xl shadow-gray-200/50">
                        <div class="p-3 text-gray-400">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-6 h-6">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607z" />
                            </svg>
                        </div>
                        <input 
                            type="text" 
                            placeholder="جستجو در مقالات..." 
                            class="bg-transparent border-none outline-none text-gray-700 w-full placeholder-gray-400 text-base px-2 font-medium"
                        />
                        <button class="bg-doramed-primary hover:bg-doramed-dark text-white px-6 py-3 rounded-xl font-bold text-sm transition-all shadow-md hover:shadow-lg">
                            جستجو
                        </button>
                    </div>
                </div>
            </div>

            {featuredPost && (
                <div class="mb-24">
                    <div class="flex items-center gap-3 mb-6">
                        <div class="h-8 w-1.5 bg-doramed-primary rounded-full"></div>
                        <h3 class="text-gray-900 font-bold text-2xl">مقاله برجسته</h3>
                    </div>

                    <a href={`/blog/${featuredPost.slug}`} class="group relative block w-full bg-white border border-gray-100 rounded-[2.5rem] overflow-hidden shadow-2xl shadow-gray-200/50 hover:shadow-doramed-primary/20 transition-all duration-500 hover:-translate-y-1">
                        <div class="grid lg:grid-cols-2 gap-0">
                            <div class="relative h-[350px] lg:h-[500px] overflow-hidden">
                                <Image 
                                    src={featuredPost.data.image} 
                                    alt={featuredPost.data.title} 
                                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-1000 ease-out"
                                    transition:name={`img-${featuredPost.slug}`}
                                />
                                <div class="absolute top-6 right-6 z-20">
                                    <span class="bg-white/90 backdrop-blur-md text-doramed-primary text-xs font-bold px-4 py-2 rounded-full shadow-lg">
                                        منتخب سردبیر 🔥
                                    </span>
                                </div>
                            </div>

                            <div class="p-8 lg:p-14 flex flex-col justify-center">
                                <div class="flex items-center gap-3 mb-6">
                                    <span class="text-doramed-primary text-sm font-bold bg-blue-50 px-3 py-1 rounded-lg">
                                        {featuredPost.data.tags[0]}
                                    </span>
                                    <span class="text-gray-400 text-sm">{formatDate(featuredPost.data.pubDate)}</span>
                                </div>

                                <h2 class="text-3xl lg:text-4xl font-black text-gray-900 mb-6 leading-tight group-hover:text-doramed-primary transition-colors">
                                    {featuredPost.data.title}
                                </h2>
                                
                                <p class="text-gray-500 text-lg leading-relaxed mb-8 line-clamp-3">
                                    {featuredPost.data.description}
                                </p>

                                <div class="flex items-center gap-2 text-doramed-dark font-bold mt-auto group-hover:gap-4 transition-all">
                                    <span>مطالعه کامل مقاله</span>
                                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5 text-doramed-primary">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5L3 12m0 0l7.5-7.5M3 12h18" />
                                    </svg>
                                </div>
                            </div>
                        </div>
                    </a>
                </div>
            )}

            <div>
                <div class="flex items-center gap-3 mb-10">
                    <div class="h-2 w-2 bg-gray-300 rounded-full"></div>
                    <h3 class="text-gray-900 font-bold text-xl">آخرین نوشته‌ها</h3>
                </div>
                
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    {otherPosts.map(post => (
                        <a href={`/blog/${post.slug}`} class="group flex flex-col h-full bg-white border border-gray-100 rounded-3xl overflow-hidden shadow-lg shadow-gray-100 hover:shadow-2xl hover:shadow-gray-200 hover:-translate-y-2 transition-all duration-300">
                            
                            <div class="h-60 relative overflow-hidden">
                                <Image 
                                    src={post.data.image} 
                                    alt={post.data.title} 
                                    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"
                                    transition:name={`img-${post.slug}`}
                                />
                                <div class="absolute top-4 right-4">
                                     <span class="text-xs font-bold text-gray-800 bg-white/90 backdrop-blur px-3 py-1.5 rounded-lg shadow-sm">
                                        {post.data.tags[0]}
                                    </span>
                                </div>
                            </div>

                            <div class="p-7 flex flex-col flex-grow">
                                <div class="flex justify-between items-center text-xs text-gray-400 mb-4 font-medium">
                                    <span>{formatDate(post.data.pubDate)}</span>
                                    <span class="bg-gray-50 px-2 py-1 rounded">۵ دقیقه مطالعه</span>
                                </div>
                                
                                <h2 class="text-xl font-bold text-gray-900 mb-3 line-clamp-2 leading-snug group-hover:text-doramed-primary transition-colors">
                                    {post.data.title}
                                </h2>
                                
                                <p class="text-gray-500 text-sm line-clamp-3 mb-6 flex-grow leading-relaxed">
                                    {post.data.description}
                                </p>
                                
                                <div class="mt-auto pt-5 border-t border-gray-100 flex items-center justify-between">
                                    <span class="text-sm font-bold text-doramed-primary opacity-0 group-hover:opacity-100 transition-opacity duration-300 -translate-x-2 group-hover:translate-x-0">
                                        ادامه مطلب
                                    </span>
                                    <div class="w-10 h-10 rounded-full bg-gray-50 flex items-center justify-center text-gray-400 group-hover:bg-doramed-primary group-hover:text-white transition-all duration-300 shadow-sm">
                                         <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5" />
                                        </svg>
                                    </div>
                                </div>
                            </div>
                        </a>
                    ))}
                </div>
            </div>

        </div>
    </main>

    <Footer />
</Layout>