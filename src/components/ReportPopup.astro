---
// src/components/ReportPopup.astro
---

<div id="report-popup" class="fixed inset-0 z-[100] hidden items-center justify-center px-4 font-sans" role="dialog" aria-modal="true">
    
    <div id="popup-backdrop" class="absolute inset-0 bg-black/60 backdrop-blur-sm transition-opacity duration-700 ease-out opacity-0"></div>

    <div id="popup-content" class="relative w-full max-w-sm bg-doramed-darker/90 backdrop-blur-2xl border border-white/10 rounded-[2.5rem] shadow-2xl overflow-hidden transform scale-90 translate-y-8 opacity-0 transition-all duration-700 cubic-bezier(0.34, 1.56, 0.64, 1)">
        
        <div class="absolute top-[-50%] left-[-50%] w-[200%] h-[200%] bg-[conic-gradient(from_0deg,transparent_0_340deg,rgba(6,182,212,0.2)_360deg)] animate-spin-slow opacity-60 pointer-events-none"></div>
        <div class="absolute inset-[1px] bg-doramed-darker rounded-[2.5rem] z-0"></div>

        <div class="relative z-10 p-8 flex flex-col items-center text-center">
            
            <button id="close-popup" class="absolute top-5 right-5 p-2 text-white/30 hover:text-white hover:bg-white/10 rounded-full transition-all duration-300 group" aria-label="بستن">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5 group-hover:rotate-90 transition-transform">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>

            <div class="relative mb-6 group cursor-default">
                <div class="absolute inset-0 bg-cyan-500/20 blur-2xl rounded-full group-hover:bg-cyan-500/30 transition-all duration-500"></div>
                <div class="w-24 h-24 bg-gradient-to-tr from-doramed-primary via-doramed-primary to-cyan-500 rounded-3xl rotate-6 flex items-center justify-center shadow-2xl shadow-cyan-500/30 border border-white/10 group-hover:rotate-12 group-hover:scale-105 transition-all duration-500">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="white" class="w-12 h-12 -rotate-6 drop-shadow-lg group-hover:-rotate-12 transition-all">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 14.25v-2.625a3.375 3.375 0 00-3.375-3.375h-1.5A1.125 1.125 0 0113.5 7.125v-1.5a3.375 3.375 0 00-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 00-9-9z" />
                    </svg>
                </div>
                <div class="absolute -top-1 -right-1 bg-red-500 text-white text-[10px] font-bold px-2.5 py-1 rounded-full border-4 border-doramed-darker shadow-lg animate-bounce">
                    توجه
                </div>
            </div>

            <h3 class="text-2xl font-black text-white mb-3 tracking-tight">بازدیدکننده گرامی</h3>
            
            <p class="text-doramed-gray text-sm leading-7 mb-8 px-1">
                اگر اخیراً چکاپ انجام داده‌اید، برای مشاهده <span class="text-cyan-400 font-bold border-b border-cyan-400/30">گزارش تجمیعی هوشمند</span> خود روی دکمه زیر کلیک کنید.
                <span class="block mt-2 text-xs text-white/40">(مخصوص مراجعین کیوسک‌های دورامد)</span>
            </p>

            <a href="http://kiosk.doramed.ir/subusers/login" class="w-full py-4 bg-gradient-to-r from-doramed-primary to-cyan-600 text-white font-bold rounded-2xl shadow-xl shadow-cyan-500/20 hover:shadow-cyan-500/40 hover:-translate-y-1 hover:scale-[1.02] active:scale-95 transition-all duration-300 flex items-center justify-center gap-3 group overflow-hidden relative border border-white/10">
                <div class="absolute inset-0 bg-white/20 translate-x-[-100%] skew-x-12 group-hover:translate-x-[100%] transition-transform duration-700 ease-in-out"></div>
                <span class="relative z-10">مشاهده گزارش من</span>
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-5 h-5 relative z-10 group-hover:translate-x-1 transition-transform">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
                </svg>
            </a>
            
            <button id="secondary-close" class="mt-4 text-xs text-white/40 hover:text-white transition-colors border-b border-transparent hover:border-white/40 pb-0.5">
                خیر، فقط می‌خواهم سایت را ببینم
            </button>

        </div>
    </div>
</div>

<style>
    .animate-spin-slow { animation: spin 10s linear infinite; }
    @keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
    .cubic-bezier { transition-timing-function: cubic-bezier(0.34, 1.56, 0.64, 1); }
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const popup = document.getElementById('report-popup');
        const backdrop = document.getElementById('popup-backdrop');
        const content = document.getElementById('popup-content');
        const closeBtn = document.getElementById('close-popup');
        const secCloseBtn = document.getElementById('secondary-close');

        const showPopup = () => {
            if (!popup || !backdrop || !content) return;
            popup.classList.remove('hidden');
            popup.classList.add('flex');
            setTimeout(() => {
                backdrop.classList.remove('opacity-0');
                content.classList.remove('scale-90', 'translate-y-8', 'opacity-0');
                content.classList.add('scale-100', 'translate-y-0', 'opacity-100');
            }, 50);
        };

        const hidePopup = () => {
            if (!popup || !backdrop || !content) return;
            backdrop.classList.add('opacity-0');
            content.classList.remove('scale-100', 'translate-y-0', 'opacity-100');
            content.classList.add('scale-90', 'translate-y-8', 'opacity-0');
            setTimeout(() => {
                popup.classList.add('hidden');
                popup.classList.remove('flex');
            }, 700);
        };

        setTimeout(showPopup, 1500);

        closeBtn?.addEventListener('click', hidePopup);
        secCloseBtn?.addEventListener('click', hidePopup);
        backdrop?.addEventListener('click', hidePopup);
    });
</script>